
public class BST
{
	/*
	private Element element;
	private BST root;
	private BST left;
	private BST right;
	BST search(Element element, BST root)
	{
		//if()
		
		return root;
	}
	
	BST insert(Element element)
	{
		if(root == null)
		{
			//create tree
			return root;
		}
		return insert(element,root);
	}
	
	BST insert(Element element, BST root)
	{
		if(element.key == root.element.key)
		{
			
			return root;
		}else if(element.key < root.element.key)
		{
			return insert(element, root.left);
		}else if(element.key > root.element.key)
		{
			return insert(element, root.right);
		}else{
			
		}
	}
	
	*/
}
