To run, compile all files in src folder in Java 8. 

Sample data is added - note sometimes messages in the sample data are skipped because they are added too fast.

Sample data can be removed in driver by remove the method call.