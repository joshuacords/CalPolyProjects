import java.util.*;

//creates decks, reads in decks, stores decks, edits decks
public class DeckManager {
	DeckEditor de = new DeckEditor();
	List decks = new ArrayList<Deck>();
	public void readIn(String filename)
	{
		//reads in deck from a file
	}
}
